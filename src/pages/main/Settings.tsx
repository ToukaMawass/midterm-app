function Settings() {
  return (
    <div style={{ padding: "20px" }}>
      <h1>Settings</h1>

      <p>Theme settings (UI only)</p>
      <p>Profile settings (mock)</p>
    </div>
  );
}

export default Settings;